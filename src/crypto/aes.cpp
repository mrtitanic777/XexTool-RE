// aes.cpp - textbook AES-128 (FIPS-197). Clarity over speed.
#include "aes.h"
#include <cstring>

namespace xex::crypto {
namespace {

const uint8_t S[256] = {
};

uint8_t IS[256];
bool g_inv_built = [](){ for (int i=0;i<256;++i) IS[S[i]]=uint8_t(i); return true; }();

inline uint8_t xtime(uint8_t x){ return uint8_t((x<<1) ^ ((x>>7)*0x1b)); }
inline uint8_t mul(uint8_t a, uint8_t b){
    uint8_t r=0;
    while(b){ if(b&1) r^=a; a=xtime(a); b>>=1; }
    return r;
}

} // namespace

Aes128::Aes128(const uint8_t key[16]) {
    static const uint8_t rcon[10] = {0x01,0x02,0x04,0x08,0x10,0x20,0x40,0x80,0x1b,0x36};
    uint8_t* w = enc_keys_;
    std::memcpy(w, key, 16);
    for (int i = 16, r = 0; i < 176; i += 4) {
        uint8_t t[4] = { w[i-4], w[i-3], w[i-2], w[i-1] };
        if (i % 16 == 0) {
            uint8_t tmp = t[0];
            t[0] = uint8_t(S[t[1]] ^ rcon[r++]);
            t[1] = S[t[2]];
            t[2] = S[t[3]];
            t[3] = S[tmp];
        }
        for (int j = 0; j < 4; ++j) w[i+j] = uint8_t(w[i-16+j] ^ t[j]);
    }
    // Decryption uses the same schedule consumed in reverse; keep a copy.
    std::memcpy(dec_keys_, enc_keys_, 176);
}

void Aes128::encrypt_block(const uint8_t in[16], uint8_t out[16]) const {
    uint8_t s[16];
    for (int i=0;i<16;++i) s[i] = uint8_t(in[i] ^ enc_keys_[i]);
    for (int round=1; round<=10; ++round) {
        // SubBytes
        for (int i=0;i<16;++i) s[i] = S[s[i]];
        // ShiftRows
        uint8_t t[16];
        for (int r=0;r<4;++r) for (int c=0;c<4;++c) t[r+4*c] = s[r+4*((c+r)&3)];
        std::memcpy(s,t,16);
        // MixColumns (skip on last round)
        if (round != 10) {
            for (int c=0;c<4;++c) {
                uint8_t* col = s + 4*c;
                uint8_t a0=col[0],a1=col[1],a2=col[2],a3=col[3];
                col[0]=uint8_t(xtime(a0)^(xtime(a1)^a1)^a2^a3);
                col[1]=uint8_t(a0^xtime(a1)^(xtime(a2)^a2)^a3);
                col[2]=uint8_t(a0^a1^xtime(a2)^(xtime(a3)^a3));
                col[3]=uint8_t((xtime(a0)^a0)^a1^a2^xtime(a3));
            }
        }
        for (int i=0;i<16;++i) s[i] ^= enc_keys_[16*round+i];
    }
    std::memcpy(out, s, 16);
}

void Aes128::decrypt_block(const uint8_t in[16], uint8_t out[16]) const {
    uint8_t s[16];
    for (int i=0;i<16;++i) s[i] = uint8_t(in[i] ^ dec_keys_[160+i]);
    for (int round=9; round>=0; --round) {
        // InvShiftRows
        uint8_t t[16];
        for (int r=0;r<4;++r) for (int c=0;c<4;++c) t[r+4*((c+r)&3)] = s[r+4*c];
        std::memcpy(s,t,16);
        // InvSubBytes
        for (int i=0;i<16;++i) s[i] = IS[s[i]];
        // AddRoundKey
        for (int i=0;i<16;++i) s[i] ^= dec_keys_[16*round+i];
        // InvMixColumns (skip after the final/round-0 add)
        if (round != 0) {
            for (int c=0;c<4;++c) {
                uint8_t* col = s + 4*c;
                uint8_t a0=col[0],a1=col[1],a2=col[2],a3=col[3];
                col[0]=uint8_t(mul(a0,14)^mul(a1,11)^mul(a2,13)^mul(a3,9));
                col[1]=uint8_t(mul(a0,9)^mul(a1,14)^mul(a2,11)^mul(a3,13));
                col[2]=uint8_t(mul(a0,13)^mul(a1,9)^mul(a2,14)^mul(a3,11));
                col[3]=uint8_t(mul(a0,11)^mul(a1,13)^mul(a2,9)^mul(a3,14));
            }
        }
    }
    std::memcpy(out, s, 16);
}

void Aes128::cbc_encrypt(uint8_t* data, size_t len, uint8_t iv[16]) const {
    for (size_t off=0; off+16<=len; off+=16) {
        for (int i=0;i<16;++i) data[off+i] ^= iv[i];
        encrypt_block(data+off, data+off);
        std::memcpy(iv, data+off, 16);
    }
}

void Aes128::cbc_decrypt(uint8_t* data, size_t len, uint8_t iv[16]) const {
    uint8_t next_iv[16];
    for (size_t off=0; off+16<=len; off+=16) {
        std::memcpy(next_iv, data+off, 16);
        decrypt_block(data+off, data+off);
        for (int i=0;i<16;++i) data[off+i] ^= iv[i];
        std::memcpy(iv, next_iv, 16);
    }
}

} // namespace xex::crypto
